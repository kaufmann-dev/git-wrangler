---
title: "version"
description: "Prints Git Wrangler version metadata."
category: "Utility"
order: 5
usage: "git-wrangler version"
---

# version

Prints the Git Wrangler version, commit, and build date.

## Usage

```bash
git-wrangler version
```

The root `--version` flag prints the same metadata:

```bash
git-wrangler --version
```
