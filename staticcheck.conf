# git-wrangler prints error strings directly to users as complete sentences,
# so terminal punctuation is an intentional, consistent UX choice. Disable the
# "error strings should not end with punctuation" check; keep everything else.
checks = ["inherit", "-ST1005"]
